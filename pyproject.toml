[project]
name = "quest"
version = "0.1.0"
description = "Gamified task management for Claude Code"
requires-python = ">=3.11"
dependencies = ["click>=8.0", "textual>=1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
